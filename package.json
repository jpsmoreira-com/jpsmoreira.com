{
  "name": "jpsmoreira.com",
  "version": "1.0.0",
  "private": true,
  "description": "Personal professional website of João Moreira",
  "scripts": {
    "build": "node build.mjs",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4"
  },
  "dependencies": {
    "marked": "^18.0.9"
  }
}
