---
title: Your Article Title
date: 2026-01-31
type: article
description: One-line summary — shown in the homepage list and the RSS feed.
lede: Longer subtitle shown under the article title on its page. Falls back to description if omitted.
---

To publish: copy this file to `content/<slug>.md` — the filename becomes the URL
(`/articles/<slug>`) — fill in the front-matter above, write the body below in
Markdown, delete these instructions, and push.

## Sections use H2 headings

Regular Markdown works: **bold**, *italics*, `inline code`, links, lists, and
fenced code blocks. Raw HTML blocks pass through untouched — that's how you embed
`<figure>` + inline SVG diagrams (see the published articles for examples that
use the site's CSS variables so they adapt to dark mode).
